<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <!-- Подключаем Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
</head>
<body class="container mt-5">

    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="/">Home</a>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="/register">Register</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/login">Login</a>
                </li>
            </ul>
        </div>
    </nav>
    <h1>Поиск книг с использованием Open Library API</h1>
    <form action="/search" method="GET">
        <input type="text" name="q" placeholder="Введите название книги" required>
        <button type="submit">Поиск</button>
    </form>
    <div>

        <% if (books) { %>
            <% books.forEach(function(book) { %>
                <p><%= book.title %> by <%= book.author_name ? book.author_name.join(', ') : 'Unknown Author' %> published in <%= book.first_publish_year %></p>
            <% }); %>
        <% } %>
    </div>

    <h1>Введите ID для получения информации</h1>
<form action="/fetch-info" method="GET">
    <input type="text" name="id" placeholder="Введите ID" required>
    <button type="submit">Получить информацию</button>
</form>
<div id="info">
    <% if (info) { %>
        <p>Short Name: <%= info.short_name %></p>
        <p>Name: <%= info.name %></p>
        <p>Status: <%= info.status %></p>
        <p>Decimals: <%= info.decimals %></p>
        <p>Points: <%= info.points %></p>
        <p>Base Currency: <%= info.base_curr %></p>
        <p>Quote Currency: <%= info.quote_curr %></p>
        <p>Category: <%= info.category %></p>
        <p>Type: <%= info.type %></p>
    <% } %>
</div>


    <h1>HOME</h1>

    <p>You can register <a href="register">Register</a></p>
    <p>You can login <a href="login">Login</a></p>

   
    <!-- Подключаем Bootstrap JS и jQuery -->
    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
</body>
</html>
